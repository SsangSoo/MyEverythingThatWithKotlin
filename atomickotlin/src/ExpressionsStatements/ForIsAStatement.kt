package ExpressionsStatements

fun main() {
    // 다음과 같이 할 수는 없다
//     val f = for(i in 1..10) {} // For is not an expression, and only expressions are allowed here
//     컴파일러 오류 메시지:
    // for is not an expression, and
    // only expressions are allowed here
}